# ショート動画 採点手順（だれでも使える版）

完成したショート動画を「投稿OK / 保留 / 不採用」で自動判定するための手順。
採点の中身（配点・チェック項目）は `ショート動画採点基準.md` を参照。

## 全体の流れ

```
動画ファイル
   │  ① analyze_short.sh で素材を自動抽出
   ▼
素材（メタ情報 + 品質チェック + 文字起こし + 静止画）
   │  ② 素材をAI採点プロンプトに貼る
   ▼
AIが採点 → 各軸の点数 + 根拠 + 改善提案 + 最終判定
```

AIは動画を直接「見られない」ため、いったん**文字起こしと静止画に変換**してから渡すのがポイント。

---

## 0. 前提ソフトのインストール（初回のみ）

`ffmpeg`（動画処理）と `whisper`（音声→文字起こし、ローカル実行で無料）が必要。

```bash
# macOS（Homebrew）
brew install ffmpeg
pip install -U openai-whisper
```

確認:

```bash
ffmpeg -version   # 出ればOK
whisper --help    # 出ればOK
```

> whisper はローカルで動くので**API課金は発生しない**。初回はモデルのダウンロードが走る。

---

## 0.5. ターゲットを確認する（初回・チャンネル変更時は必須）

採点の「④ ターゲット一致度」は `ターゲット定義.md` の A・B・C に基づいて判定する。
**初めて使う人、別チャンネルで使う人は、採点する前に必ず中身を確認・書き換えること。**

- `ターゲット定義.md` を開く
- 「このチャンネルは“誰の”“どんな悩み”を解決する動画か？」を自問する
- サンプル（AI活用チャンネル想定）と違う場合は A・B・C を自分のチャンネル用に書き換える

> ここがズレると④の点数がそのままズレるため、採点精度に直結する。
> 2回目以降、同じチャンネルなら確認だけでよい。

---

## 1. 素材を抽出する

採点したい動画を引数にして実行するだけ。

```bash
cd /Users/kz5/Cursor/YouTube/short-judgment
chmod +x analyze_short.sh   # 初回のみ
./analyze_short.sh "/path/to/動画.mp4"
```

オプション:

```bash
# 出力先と文字起こし精度を指定（medium は精度↑だが時間↑）
./analyze_short.sh "/path/to/動画.mp4" ./analysis medium
```

実行すると `analysis/<動画名>/` に以下が出力される。

| 出力 | 内容 |
|---|---|
| `info.txt` | 解像度・尺・音量・黒フレーム・無音区間（＝映像音声品質の判定材料） |
| `transcript.txt` | 音声の文字起こし全文（＝フック・感情・完結性・ターゲットの判定材料） |
| `transcript_timed.tsv` | セグメント単位の開始終了ミリ秒つき文字起こし（＝**冒頭2秒の機械判定用**。start≤2000ms がフック範囲） |
| `frames/*.jpg` | 冒頭は密に、以降3秒ごと＋末尾の静止画（＝テロップ・画面の確認用） |

> 文字起こしは誤認識することがある（例：「失注」→「出注」）。**最終的な文言はテロップ静止画で確認**する。

---

## 2. AIに採点させる

`ショート動画採点基準.md` の「8. 運用プロンプト」をコピーし、次を差し込んで実行する。

1. プロンプト冒頭の **ターゲット視聴者 A・B・C** に `ターゲット定義.md` の内容を貼る（手順 0.5 で確認済みのもの）
2. `transcript.txt` の中身を貼る
3. `info.txt` の品質情報を貼る
4. `frames/` の静止画（特に冒頭とテロップが映るもの）を添付

AIが各軸の点数・根拠の引用・改善提案・最終判定を返す。

---

## 3. 判定の読み方

`ショート動画採点基準.md` の通り:

```
70点以上 → 投稿OK
50〜69点 → 保留（冒頭テキストの工夫で救える可能性あり）
49点以下 → 不採用

※ 投稿OKは次を全て満たすこと:
  合計70点以上 / 冒頭フック20点以上 / 映像音声で即0点なし
```

---

## 4. 採点結果を保存する（必須）

採点基準 `9. 機械可読JSON出力` に従い、全動画のJSON出力が終わったら保存を実行する。

### 手順

```bash
# 1. ##SCORES_JSON_START## ～ ##SCORES_JSON_END## の間のJSONを一時ファイルに書き出す
#    （Claudeがファイルに書き出した上で以下を実行）

# 2. save_scores.py を実行（軸別スコア・個別score.json・scores.json の3箇所に保存）
cd /Users/kz5/ClaudeCode/short-pipeline
.venv/bin/python save_scores.py \
  --source "元動画タイトル" \
  --scores-file /tmp/scores_draft.json

# CSV も出力したい場合（動画フォルダを指定）
.venv/bin/python save_scores.py \
  --source "元動画タイトル" \
  --scores-file /tmp/scores_draft.json \
  --video-dir /path/to/動画フォルダ
```

### 保存先

| 場所 | 内容 |
|---|---|
| `data/scores.json` | 全セッション横断の採点履歴（軸別スコア含む） |
| `data/analysis/{動画名}/score.json` | 動画単体の詳細採点（後から単体で参照用） |
| `{動画フォルダ}/scores.csv` | ランキング形式のサマリ（--video-dir 指定時） |

---

## 補足：精度を上げる運用

- 判定が体感とズレた動画はメモしておき、`ショート動画採点基準.md` の配点・チェック項目を少しずつ調整する。
- ターゲット A・B・C を一度確定させると、ターゲット一致度のブレが減る。
- 文字起こし精度が足りないときは whisper モデルを `small` → `medium` に上げる。
